opencollection: 1.0.0

info:
  name: sohojogi-api
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  auth:
    type: bearer
    token: "{{accessToken}}"
  variables:
    - name: baseUrl
      value: https://server.sohojogi.com.bd/api/v1
    - name: baseUrl
      value: http://localhost:5000/api/v1
      disabled: true
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
