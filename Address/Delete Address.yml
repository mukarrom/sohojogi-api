info:
  name: Delete Address
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/addresses/{{addressId}}"
  params:
    - name: addressId
      value: "{{addressId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
